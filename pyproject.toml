[project]
name = "vigilant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Carlota de la Vega", email = "carlotadlavega@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
vigilant = "vigilant:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
